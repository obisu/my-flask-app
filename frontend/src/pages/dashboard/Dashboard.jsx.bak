import React from "react";
import { Link } from "react-router-dom";

export default function Dashboard() {
  return (
    <div style={{ padding: "2rem" }}>
      {/* Header + Logout */}
      <div style={{ display: "flex", justifyContent: "space-between", alignItems: "center" }}>
        <h1 style={{ marginBottom: "1rem" }}>Dashboard</h1>

        <button
          onClick={() => {
            localStorage.removeItem("loggedIn");
            window.location.href = "/login";
          }}
          style={{
            padding: "0.5rem 1rem",
            background: "#d9534f",
            color: "white",
            border: "none",
            borderRadius: "4px",
            cursor: "pointer"
          }}
        >
          Logout
        </button>
      </div>

      {/* Navigation Section */}
      <div
        style={{
          marginBottom: "2rem",
          padding: "1.5rem",
          background: "#ffffff",
          borderRadius: "8px",
          boxShadow: "0 2px 6px rgba(0,0,0,0.1)",
        }}
      >
        <h3>Navigation</h3>
        <ul>
          <li><Link to="/add-user">Add User</Link></li>
          <li><Link to="/users">View Users</Link></li>
          <li><Link to="/db-test">Database Test</Link></li>
        </ul>
      </div>

      {/* Dashboard Cards */}
      <div
        style={{
          display: "grid",
          gridTemplateColumns: "repeat(auto-fit, minmax(250px, 1fr))",
          gap: "1.5rem",
        }}
      >
        <div
          style={{
            padding: "1.5rem",
            background: "#ffffff",
            borderRadius: "8px",
            boxShadow: "0 2px 6px rgba(0,0,0,0.1)",
          }}
        >
          <h3>System Status</h3>
          <p>Backend: <strong>ok</strong></p>
          <p>Frontend: <strong>ok</strong></p>
          <p>Cluster: <strong>healthy</strong></p>
        </div>

        <div
          style={{
            padding: "1.5rem",
            background: "#ffffff",
            borderRadius: "8px",
            boxShadow: "0 2px 6px rgba(0,0,0,0.1)",
          }}
        >
          <h3>User Info</h3>
          <p>Logged in as: <strong>admin</strong></p>
          <p>Role: <strong>Administrator</strong></p>
          <p>Session: <strong>Active</strong></p>
        </div>

        <div
          style={{
            padding: "1.5rem",
            background: "#ffffff",
            borderRadius: "8px",
            boxShadow: "0 2px 6px rgba(0,0,0,0.1)",
          }}
        >
          <h3>Next Steps</h3>
          <ul>
            <li>Integrate PostgreSQL database</li>
            <li>Add real API data to dashboard</li>
            <li>Create user management page</li>
            <li>Add charts or analytics</li>
          </ul>
        </div>
      </div>
    </div>
  );
}

